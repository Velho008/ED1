#include <stdio.h>

/*  toda analise pode ter um melhor caso, pior caso e caso medio
    melhor caso é quando o algoritmo roda o menor numero de vezes possivel
    pior caso, geralmente o mais util, é quando o algoritmo roda o maior numero
    caso medio é estatistico   

    O(f(n))→ Limite superior (pior caso ou "no máximo")
    Ω(f(n))→ Limite inferior (melhor caso ou "no mínimo")
    Θ(f(n))→ Limite apertado (quando melhor e pior caso têm a mesma ordem)
    */